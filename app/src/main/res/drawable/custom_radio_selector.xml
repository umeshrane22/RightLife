<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- When checked -->
    <item android:drawable="@drawable/checked_radio" android:state_checked="true" />

    <!-- When unchecked -->
    <item android:drawable="@drawable/unchecked_radio" />
</selector>
