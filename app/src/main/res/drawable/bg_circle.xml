<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Circle Color -->
    <solid android:color="@color/menuselected" /> <!-- Red color -->

    <!-- Optional: Add stroke if needed -->
    <stroke
        android:width="1dp"
        android:color="@color/menuselected" />
</shape>
