<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F9F9F9"
    android:padding="16dp">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/ivBack"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/icon_back"
            android:layout_marginEnd="8dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Export All Health Data"
            android:textSize="18sp"
            android:fontFamily="@font/dmsans_bold"
            android:textColor="@color/txt_color_mind"
            android:layout_marginStart="4dp" />
    </LinearLayout>

    <!-- Content -->
    <TextView
        android:id="@+id/export_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="24dp"
        android:fontFamily="@font/dmsans_regular"
        android:text="Your health journey, your data. Get a complete record of your wellness data, activity, nutrition, sleep, and more—all in your mailbox within 24–48 hours. Secure &amp; Private – Your data remains protected."
        android:textColor="@color/txt_color_header"
        android:textSize="16sp"
        android:lineSpacingExtra="4dp" />

    <!-- Spacer -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- Export Button -->
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:background="@color/white"
        android:elevation="8dp">

        <!-- Export Button -->
        <Button
            android:id="@+id/btnExport"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Export"
            android:backgroundTint="@color/menuselected"
            android:textColor="@color/white"
            android:layout_gravity="center"
            android:layout_marginHorizontal="50dp"
            android:fontFamily="@font/dmsans_regular"
            android:textSize="18sp"
            android:padding="10dp" />
    </androidx.cardview.widget.CardView>
</LinearLayout>
